#!/bin/sh
branch="$(git symbolic-ref --short HEAD)"

if [ "$branch" = "main" ]; then
  echo "On main: bumping patch version..."
  npm version patch --no-git-tag-version
else
  echo "Not on main: skipping version bump."
fi
